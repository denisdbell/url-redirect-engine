<%= form_with(model: redirect_url, local: true) do |form| %>
  <% if redirect_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(redirect_url.errors.count, "error") %> prohibited this redirect_url from being saved:</h2>

      <ul>
      <% redirect_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :old_url %>
    <%= form.text_field :old_url, id: :redirect_url_old_url %>
  </div>

  <div class="field">
    <%= form.label :new_url %>
    <%= form.text_field :new_url, id: :redirect_url_new_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
